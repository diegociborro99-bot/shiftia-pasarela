# Variables del servidor (Railway → Variables). Copia lo que necesites; ver DEPLOY-SERVIDOR.md.
DATA_DIR=/data                      # con un Volume montado en /data (imprescindible)
SESSION_SECRET=cambia-esto-por-una-cadena-larga-aleatoria
ADMIN_PASSWORD=                     # contraseña inicial del encargado (admin); vacía = genérica con cambio obligatorio
PROGRAMADOR_USUARIO=diego
PROGRAMADOR_PASSWORD=               # vacía = provisional 12345678 (cámbiala desde Cuenta)
# JOSE_USUARIO=joseadmin          # cuenta del jefe (permisos de encargado, sin Actividad)
JOSE_PASSWORD=                     # vacía = genérica con cambio obligatorio
# PASSWORD_GENERICA=pasarela2026
# SHIFTIA_CORE_URL=https://tu-nucleo.up.railway.app
# SHIFTIA_CORE_KEY=
# SHIFTIA_CORE_TIMEOUT_S=30
# HOST_CANONICO=turnos.grupopasarela.es
# TRUST_PROXY=1
