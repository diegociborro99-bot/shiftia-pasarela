{
  "name": "shiftia-pasarela",
  "version": "0.1.1",
  "private": true,
  "description": "Shiftia · Grupo Pasarela — planificación de turnos de los cuatro locales (app + servidor sin dependencias)",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "start": "node server.js",
    "build": "node tools/build.mjs",
    "build:check": "node tools/build.mjs --check",
    "test": "node modelo.test.js && node --test tests/*.test.mjs && node tools/check-parity.mjs"
  }
}
