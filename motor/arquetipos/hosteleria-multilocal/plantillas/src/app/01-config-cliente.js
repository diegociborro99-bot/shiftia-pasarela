// ================= CONFIGURACIÓN DE LA BURBUJA (la escribe el motor de creación) =================
// Sale de cliente.json. La navegación (31-navegacion.js) y el menú «Más» del móvil leen
// `modulos` a través de moduloActivo(k) (01-core-utils.js): un módulo en false no tiene
// pestaña ni botones que lleven a él. Para cambiar algo, edita cliente.json y vuelve a
// generar la burbuja, o toca esto a mano y ejecuta el build.
const CONFIG_CLIENTE = {{json configCliente}};
