{
  "name": "shiftia-{{slug}}",
  "version": "0.1.0",
  "private": true,
  "description": "Shiftia · {{nombre}} — {{descripcion}} (app + servidor sin dependencias)",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "start": "node server.js",
    "build": "node tools/build.mjs",
    "build:check": "node tools/build.mjs --check",
    "test": "node modelo.test.js && node --test tests/*.test.mjs && node tools/check-parity.mjs",
    "test:e2e": "for f in tests/e2e-*.mjs; do node \"$f\" || exit 1; done"
  }
}
